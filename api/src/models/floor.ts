class floor {
    id : number = null;
    name_floor : string = null;

    constructor(id : number = null, name_floor : string = null) {
        this.id = id;
        this.name_floor = name_floor;
    }
}

export default floor;